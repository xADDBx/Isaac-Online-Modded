<Window x:Class="IsaacModInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Isaac Online Modded" Height="260" Width="842" MinWidth="600" MinHeight="260">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Game Path:"/>
        <TextBox Name="txtGamePath" Grid.Row="1" Height="26" Margin="0,5,8,8"
                 VerticalContentAlignment="Center" TextChanged="GamePath_TextChanged"/>
        <Button Grid.Row="1" Grid.Column="1" Content="Browse" Width="80" Margin="0,5,0,8"
                Click="BrowseButton_Click"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Patch Game" Width="110" Click="PatchButton_Click"/>
            <Button Name="btnPatchCoopCharacters" Content="Patch Coop Characters (Optional)" Width="220"
                    Margin="10,0,0,0" Click="CoopCharactersButton_Click"
                    ToolTip="Requires the Coop patch."/>
            <Button Content="Patch External Item Descriptions" Width="220" Margin="10,0,0,0"
                    Click="EIDButton_Click"/>
        </StackPanel>

        <Border Grid.Row="3" Grid.ColumnSpan="2" BorderBrush="#D0D0D0" BorderThickness="1" Padding="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="EID Patched: "/>
                <TextBlock Name="txtEidPatchStatus" FontWeight="SemiBold" Margin="0,0,24,0"/>
                <TextBlock Text="Coop Patched: "/>
                <TextBlock Name="txtCoopPatchStatus" FontWeight="SemiBold" Margin="0,0,24,0"/>
                <TextBlock Text="Coop Characters Patched: "/>
                <TextBlock Name="txtCoopCharactersPatchStatus" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <TextBlock Name="txtStatus" Grid.Row="4" Grid.ColumnSpan="2" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>
